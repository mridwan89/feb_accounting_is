{
  "name": "siapkas-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Uji ujung-ke-ujung SIAPKas melalui antarmuka (Playwright: Chromium dan cangkang desktop Electron)",
  "type": "module",
  "scripts": {
    "pretest": "npm --prefix ../client run build",
    "test": "playwright test --project=chromium --project=desktop",
    "test:desktop": "playwright test --project=desktop --no-deps",
    "pretangkapan": "npm --prefix ../client run build",
    "tangkapan": "playwright test --project=tangkapan"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
